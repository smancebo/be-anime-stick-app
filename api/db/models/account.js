const DB = require('../client');

module.exports = class Account {
    static create(account) {
        throw 'not implemented';
    }
    static update(id, account) {
        throw 'Not implemented';
    }
    static addProfile(id, profile) {
        throw 'Not implemented'
    }
}